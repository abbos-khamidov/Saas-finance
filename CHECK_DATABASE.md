# Проверка использования базы данных

## Текущая ситуация

Приложение использует **гибридный подход**:

1. **Django API + SQLite3** (если сервер запущен) ✅
2. **localStorage** (fallback, если API недоступен) ⚠️

## Как проверить, что используется

### Вариант 1: Проверить консоль браузера

Откройте DevTools (F12) → Console:

- ✅ **Используется БД**: Нет ошибок, данные загружаются
- ⚠️ **Используется localStorage**: Видно сообщение `"API unavailable, using localStorage"`

### Вариант 2: Проверить localStorage

В DevTools → Application → Local Storage:

- Если видите ключи `finance_*_transactions`, `finance_*_settings` → используется **localStorage**
- Если ключей нет → используется **БД** (данные на сервере)

### Вариант 3: Запустить Django backend

```bash
cd backend
pip install -r requirements.txt
python manage.py migrate
python manage.py runserver
```

После запуска на `http://localhost:8000/api`:
- ✅ Все данные сохраняются в **SQLite3** (`backend/db.sqlite3`)
- ❌ Нет fallback на localStorage

## Текущее состояние

По умолчанию `dataService.js` пытается использовать API (`useAPI = true`), но если API недоступен, автоматически переключается на localStorage.

## Как сделать так, чтобы всегда использовалась БД

1. **Убедиться, что Django сервер запущен**
   ```bash
   cd backend
   python manage.py runserver
   ```

2. **Проверить URL в `.env`** (если создан):
   ```
   VITE_API_URL=http://localhost:8000/api
   ```

3. **Или в `apiService.js`** уже установлен:
   ```javascript
   const API_BASE_URL = import.meta.env.VITE_API_URL || 'http://localhost:8000/api';
   ```

## Проверка БД SQLite3

Если Django запущен, можно проверить данные:

```bash
cd backend
python manage.py shell
```

В shell:
```python
from api.models import Transaction, User
transactions = Transaction.objects.all()
print(f"Транзакций в БД: {transactions.count()}")
for t in transactions[:5]:
    print(f"{t.type}: {t.amount} - {t.date}")
```

## Где хранятся данные сейчас

**Если Django НЕ запущен:**
- 📦 `localStorage` браузера (временное хранилище)

**Если Django запущен:**
- 💾 `backend/db.sqlite3` (постоянная база данных)
