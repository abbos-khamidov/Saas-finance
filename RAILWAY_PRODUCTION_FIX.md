# Исправления для Railway Production

## ✅ Что исправлено:

### 1. Автоматическое определение API URL для продакшена
- **Файл**: `src/services/apiService.js`
- **Изменение**: Добавлена функция `getApiBaseUrl()` которая:
  - Проверяет переменную окружения `VITE_API_URL`
  - Автоматически определяет Railway домен и использует `/api` на том же домене
  - Fallback на `localhost:8000` для локальной разработки

### 2. Настройка Django для обслуживания React
- **Файл**: `backend/finance_api/urls.py`
- **Изменение**: Добавлены маршруты для обслуживания React SPA:
  - `path('', TemplateView.as_view(template_name='index.html'))` - главная страница
  - `path('<path:path>', ...)` - все остальные роуты React

### 3. Настройка шаблонов
- **Файл**: `backend/finance_api/settings.py`
- **Изменение**: Добавлена папка `templates` в `TEMPLATES['DIRS']`
- **Файл**: `backend/templates/index.html`
- **Изменение**: Пути к статике обновлены на `/static/assets/...`

### 4. Обновление Railway конфигурации
- **Файл**: `backend/railway.json`
- **Изменение**: Build command обновлен для сборки React и копирования файлов

## 🔍 Как проверить на Railway:

### 1. Проверка API
Откройте в браузере:
- `https://saas-finance-production-c945.up.railway.app/api/` - должен вернуть список доступных эндпоинтов
- `https://saas-finance-production-c945.up.railway.app/api/transactions/` - должен вернуть пустой список или ошибку (если нет user_id)

### 2. Проверка Frontend
- `https://saas-finance-production-c945.up.railway.app/` - должен загрузиться React приложение
- Откройте DevTools → Console и проверьте:
  - Нет ли ошибок загрузки JS/CSS файлов
  - Правильно ли определяется API URL (должен быть Railway домен, не localhost)

### 3. Проверка Network запросов
- Откройте DevTools → Network
- Попробуйте создать транзакцию
- Проверьте URL запроса: должен быть `https://saas-finance-production-c945.up.railway.app/api/transactions/`
- НЕ должен быть `http://localhost:8000/api/transactions/`

## ⚙️ Переменные окружения на Railway:

В Railway Dashboard → Variables должны быть установлены:

```
SECRET_KEY=your-secret-key-min-50-chars
DEBUG=False
ALLOWED_HOSTS=saas-finance-production-c945.up.railway.app,*.railway.app
```

**Важно**: `VITE_API_URL` НЕ нужна на Railway, так как теперь API URL определяется автоматически!

## 🚀 После деплоя:

1. Railway автоматически пересоберет проект
2. Проверьте логи в Railway Dashboard → Logs
3. Убедитесь, что:
   - Миграции выполнены
   - Статика собрана
   - Сервер запущен

## 📝 Если что-то не работает:

1. **404 на API запросы**:
   - Проверьте, что Django сервер запущен (логи Railway)
   - Проверьте, что URL правильный (должен быть `/api/transactions/`, не `/transactions/`)

2. **JS/CSS файлы не загружаются**:
   - Проверьте, что файлы скопированы в `backend/staticfiles/assets/`
   - Проверьте, что `collectstatic` выполнен
   - Проверьте пути в `backend/templates/index.html`

3. **API возвращает 404**:
   - Проверьте консоль браузера - там будет логирование запросов
   - Проверьте, что API URL правильный (Railway домен, не localhost)
