# Финансовый SaaS - Учёт Финансов

Премиум-приложение для учёта доходов и расходов с подпиской $9/месяц, ориентированное на фрилансеров и самозанятых.

## 🚀 Возможности

### Free план
- Базовый учёт доходов и расходов
- Просмотр транзакций
- Простая статистика

### Pro план ($9/месяц)
- ✅ **Финансовые инсайты**
  - Дневной лимит трат
  - Прогноз баланса до конца месяца
  - Сравнение с прошлым периодом
  - Перерасход по категориям

- ✅ **Бюджеты по категориям**
  - Лимиты расходов
  - Визуальные индикаторы (green/yellow/red)
  - Предупреждения о превышении

- ✅ **Расширенная аналитика**
  - Графики доходов и расходов
  - Анализ по категориям
  - Сравнительные отчёты

- ✅ **Onboarding**
  - Персональная настройка при первом входе
  - Указание дохода и обязательных расходов
  - Финансовая цель

## 🛠 Технологии

- **Frontend**: HTML5, CSS3, Vanilla JavaScript (ES6 Modules)
- **Backend**: Firebase Authentication, Cloud Firestore
- **Charts**: Chart.js
- **Hosting**: Vercel
- **Fonts**: Inter (Google Fonts)

## 📦 Установка и запуск

### Локальная разработка

1. Клонируйте репозиторий
```bash
git clone <repository-url>
cd abushka
```

2. Настройте Firebase
   - Создайте проект в [Firebase Console](https://console.firebase.google.com)
   - Включите Authentication (Email/Password)
   - Создайте Firestore Database
   - Скопируйте конфигурацию в `js/firebase-config.js`

3. Настройте Firestore Rules
```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /expenses/{expenseId} {
      allow read, write: if request.auth != null && 
                          request.auth.uid == resource.data.userId;
      allow create: if request.auth != null && 
                       request.auth.uid == request.resource.data.userId;
    }
    match /incomes/{incomeId} {
      allow read, write: if request.auth != null && 
                          request.auth.uid == resource.data.userId;
      allow create: if request.auth != null && 
                       request.auth.uid == request.resource.data.userId;
    }
    match /userSettings/{userId} {
      allow read, write: if request.auth != null && 
                          request.auth.uid == userId;
    }
  }
}
```

4. Запустите локально
   - Используйте Live Server в VS Code
   - Или любой другой статический сервер

## 🚀 Деплой на Vercel

1. Подключите репозиторий к Vercel
2. Vercel автоматически определит настройки из `vercel.json`
3. Деплой произойдёт автоматически при push в main ветку

### Environment Variables (опционально)
Если вынесли Firebase конфигурацию в переменные окружения:
- `VITE_FIREBASE_API_KEY`
- `VITE_FIREBASE_AUTH_DOMAIN`
- и т.д.

## 📁 Структура проекта

```
abushka/
├── index.html              # Главная страница
├── analytics.html          # Страница аналитики
├── auth.html              # Страница авторизации
├── onboarding.html        # Onboarding flow
├── style.css              # Стили
├── vercel.json            # Конфигурация Vercel
├── ARCHITECTURE.md        # Документация архитектуры
├── README.md              # Этот файл
└── js/
    ├── firebase-config.js # Firebase настройки
    ├── auth.js            # Логика авторизации
    ├── app.js             # Основное приложение
    ├── analytics.js        # Аналитика и графики
    ├── insights.js         # Финансовые инсайты
    ├── user-settings.js    # Управление настройками
    └── onboarding.js      # Onboarding flow
```

## 💡 Основные функции

### Финансовые инсайты

- **Дневной лимит трат**: автоматический расчёт на основе дохода и обязательных расходов
- **Прогноз до конца месяца**: предсказание баланса с учётом текущих трат
- **Сравнение периодов**: анализ изменений по сравнению с прошлым месяцем
- **Перерасход по категориям**: автоматическое обнаружение превышения бюджетов

### Бюджеты

- Установка лимитов по категориям
- Визуальные индикаторы прогресса
- Автоматические предупреждения

### Onboarding

При первом входе пользователь настраивает:
- Доход в месяц
- Обязательные расходы
- Финансовую цель (опционально)

## 🔒 Безопасность

- Firestore правила защищают данные пользователей
- Каждый пользователь видит только свои данные
- Аутентификация через Firebase Auth

## 📊 Категории расходов

- Продукты
- Транспорт
- Развлечения
- Здоровье
- Коммунальные услуги
- Одежда
- Другое

## 🎨 Дизайн

Минималистичный, профессиональный дизайн премиум-уровня:
- Тёмная тема
- Плавные анимации
- Визуальные индикаторы статуса
- Адаптивная вёрстка

## 📈 Roadmap

- [ ] Экспорт данных (CSV, JSON)
- [ ] Уведомления о превышении бюджета
- [ ] PWA для мобильных устройств
- [ ] Интеграции с банками
- [ ] Автоматическая категоризация
- [ ] AI-рекомендации

## 📝 Лицензия

Проприетарное программное обеспечение. Все права защищены.

## 👥 Поддержка

Для вопросов и предложений создавайте issues в репозитории.