# SaaS Finance App - Полнофункциональное приложение учета финансов

Профессиональное SaaS-приложение для учета финансов с Django бэкендом и React фронтендом.

## 🚀 Возможности

### Для пользователей:
- ✅ **Полный функционал без ограничений** - все возможности доступны сразу
- 💰 Учет доходов и расходов
- 📊 Глубокая аналитика с трендами
- 🎯 Система финансовых целей
- 📈 Прогнозы и инсайты
- 💵 Бюджеты по категориям
- 📱 Полностью адаптивный дизайн

### Технологии:
- **Frontend**: React + Vite + React Router
- **Backend**: Django REST Framework + SQLite3
- **Стили**: CSS3 с градиентами и анимациями
- **Деплой**: Готово для Vercel

## 📦 Установка и запуск

### Backend (Django)

```bash
cd backend
pip install -r requirements.txt
python manage.py migrate
python manage.py runserver
```

Backend будет доступен по адресу: `http://localhost:8000/api/`

### Frontend (React)

```bash
npm install
npm run dev
```

Frontend будет доступен по адресу: `http://localhost:3000`

### Переменные окружения

Создайте файл `.env` в корне проекта:

```env
VITE_API_URL=http://localhost:8000/api
```

## 📁 Структура проекта

```
├── backend/              # Django API
│   ├── api/             # API приложение
│   ├── finance_api/     # Настройки Django
│   └── manage.py        # Django CLI
├── src/                 # React приложение
│   ├── components/      # React компоненты
│   ├── pages/           # Страницы
│   ├── services/        # Сервисы (API, Auth, Data)
│   └── utils/           # Утилиты
└── package.json         # Зависимости фронтенда
```

## 🔌 API Endpoints

### Authentication
- `POST /api/users/register/` - Регистрация
- `POST /api/users/login/` - Вход

### Transactions
- `GET /api/transactions/?user_id=1` - Список транзакций
- `POST /api/transactions/` - Создать транзакцию
- `DELETE /api/transactions/{id}/` - Удалить транзакцию

### Settings
- `GET /api/settings/?user_id=1` - Настройки пользователя
- `PUT /api/settings/{id}/` - Обновить настройки

### Goals
- `GET /api/goals/?user_id=1` - Список целей
- `POST /api/goals/` - Создать цель
- `PUT /api/goals/{id}/` - Обновить цель

### Analytics
- `GET /api/analytics/{user_id}/?period=month` - Аналитика

### Insights
- `GET /api/insights/{user_id}/` - Инсайты и прогнозы

## 🎨 Дизайн

- Современный минималистичный интерфейс
- Градиенты и плавные анимации
- Адаптивность для всех устройств
- Темная/светлая тема (настраивается в CSS)

## 📊 Функции аналитики

- Статистика по периодам (день, неделя, месяц, все время)
- Разбивка по категориям
- Тренды и сравнения
- Прогнозы на месяц
- Дневные лимиты трат

## 🎯 Система целей

- Создание финансовых целей
- Отслеживание прогресса
- Установка дедлайнов
- Статусы: активна, завершена, отменена

## 🔒 Безопасность

- CORS настроен для всех источников (в разработке)
- Валидация данных на фронтенде и бэкенде
- Хеширование паролей (рекомендуется добавить в продакшен)

## 📝 Развертывание

### Vercel (Frontend)

1. Подключите репозиторий к Vercel
2. Настройте переменные окружения
3. Деплой автоматически

### Django Backend

Рекомендуется использовать:
- Heroku
- Railway
- DigitalOcean
- AWS/GCP

## 👨‍💻 Разработка

Made by **Adams Midov**

- Telegram: [@adamsmidov](https://t.me/adamsmidov)
- GitHub: [adamsmidov](https://github.com/adamsmidov)

## 📄 Лицензия

MIT License
