# Тестирование продакшена на Railway

## Проверка сайта: https://saas-finance-production-c945.up.railway.app/

### ✅ Что работает:
- Страница загружается
- Заголовок "Учёт Финансов" отображается
- Нет явных ошибок 404/500 в HTML

### ⚠️ Потенциальные проблемы:

1. **API URL на продакшене**
   - Frontend должен обращаться к `/api/` на том же домене Railway
   - Не к `localhost:8000`
   - Исправлено: добавлена автоматическая детекция Railway домена

2. **CORS настройки**
   - CORS_ALLOW_ALL_ORIGINS = True (должно работать)
   - Проверьте, что Railway домен в ALLOWED_HOSTS

3. **Переменные окружения на Railway**
   - Убедитесь, что в Railway Dashboard → Variables установлено:
     - `VITE_API_URL=https://saas-finance-production-c945.up.railway.app/api` (для frontend)
     - `SECRET_KEY=...` (для Django)
     - `DEBUG=False`
     - `ALLOWED_HOSTS=saas-finance-production-c945.up.railway.app,*.railway.app`

### 🔍 Что проверить в браузере:

1. **Откройте DevTools → Network**
   - Попробуйте создать транзакцию
   - Проверьте URL запроса к API
   - Должен быть: `https://saas-finance-production-c945.up.railway.app/api/transactions/`
   - НЕ должен быть: `http://localhost:8000/api/transactions/`

2. **Проверьте консоль браузера**
   - Ошибки JavaScript
   - Ошибки API запросов
   - Логи из apiService.js

3. **Проверьте Application → Local Storage**
   - Данные пользователя должны сохраняться
   - Если API недоступен, используется localStorage fallback

### 🛠️ Если API не работает:

1. **Проверьте Railway Dashboard → Logs**
   - Есть ли ошибки при запуске Django?
   - Запустился ли сервер?

2. **Проверьте Railway Dashboard → Settings**
   - Правильный Root Directory? (должен быть `backend` или `.`)
   - Правильный Start Command?
   - Правильные переменные окружения?

3. **Проверьте, что миграции выполнены**
   - В логах Railway должно быть: "Operations to perform: Apply all migrations"

### 📝 Рекомендации:

1. **Для монолитного деплоя (Django + React вместе)**
   - Django должен обслуживать React статику
   - Обновите `backend/finance_api/urls.py` для обслуживания React

2. **Для раздельного деплоя (2 сервиса)**
   - Backend: отдельный сервис на Railway
   - Frontend: отдельный сервис на Railway или Vercel
   - Frontend должен иметь `VITE_API_URL` указывающий на backend
